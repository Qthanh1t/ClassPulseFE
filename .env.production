# Production build values (baked by `npm run build`, mode=production).
# Relative paths → browser talks only to its own origin; Caddy reverse-proxies
# /api and /ws to the backend, /storage to MinIO. Safe to commit (no secrets).
VITE_API_BASE_URL=/api/v1
VITE_WS_URL=/ws
